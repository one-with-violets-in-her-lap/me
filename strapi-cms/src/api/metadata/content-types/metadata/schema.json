{
  "kind": "singleType",
  "collectionName": "metadata_records",
  "info": {
    "singularName": "metadata",
    "pluralName": "metadata-records",
    "displayName": "metadata"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "personStatus": {
      "type": "enumeration",
      "required": true,
      "enum": [
        "sleeping",
        "online",
        "taking a walk",
        "self-loathing"
      ]
    },
    "health": {
      "type": "integer",
      "required": true,
      "min": 0,
      "max": 100
    },
    "age": {
      "type": "integer",
      "required": true
    }
  }
}
